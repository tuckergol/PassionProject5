version: '3'
services:
        web:
                image: flask_port_v1
                build: .
                ports:
                        - "8699:8699"
                volumes:
                        - ./volumes:/volumes
                        - ./instance:/instance
                restart: unless-stopped
